{
  "name": "@signalroom/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "signalroom": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@signalroom/protocol": "*",
    "@signalroom/server": "*",
    "commander": "^14.0.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/ws": "^8.18.1"
  }
}
