services:
  - type: web
    name: signalroom-demo
    runtime: docker
    plan: free
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: SIGNALROOM_HOST
        value: 0.0.0.0
      - key: SIGNALROOM_MAX_CLIENTS
        value: 100
